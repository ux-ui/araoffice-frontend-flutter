import 'package:flutter/material.dart';

class StyleConstants {
  // Colors
  static const defaultTextColor = Colors.black;
  static const defaultBackgroundColor = Colors.transparent;
  static const defaultBorderColor = Colors.black;
  static const defaultShapeLineColor = Colors.black;

  // Animation defaults
  static const defaultAnimationDelay = 1;
  static const defaultAnimationDuration = 1;
  static const defaultAnimationRepeat = 1;
}
