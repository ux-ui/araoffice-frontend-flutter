class EditorConstants {
  // Text defaults

  static const defaultFontSize = '16pt';
  static const defaultLineWidth = 2;
  static const defaultHeadTailSize = 1;

  // Layout defaults
  static const defaultDrawerWidth = 300.0;
  static const defaultDocumentWidth = 600;
  static const defaultDocumentHeight = 800;

  // Column defaults
  static const defaultColumnCount = 1;
  static const defaultColumnGap = 10;
  static const defaultColumnRuleWidth = 1;

  // Border defaults
  static const defaultBorderWidth = 0;
  static const defaultTableBorderWidth = 1;

  // Shape types
  static const shapeTypeLine = 'line';
  static const shapeTypeRect = 'rect';
  static const shapeTypeCircle = 'circle';
  static const shapeTypeTriangle = 'triangle';
}
