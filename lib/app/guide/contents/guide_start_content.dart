import 'package:flutter/material.dart';

class GuideStartContent extends StatelessWidget {
  const GuideStartContent({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('가이드 시작하기'));
  }
}
