import 'package:flutter/material.dart';

class GuideDownloadContent extends StatelessWidget {
  const GuideDownloadContent({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('가이드 다운로드'));
  }
}
